version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - COINGECKO_API_BASE=https://api.coingecko.com/api/v3
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
